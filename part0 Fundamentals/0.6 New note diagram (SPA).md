sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note with JSON payload
    activate server
    Note right of server: Server processes the new note and saves it
    server-->>browser: 201 Created (new note saved)
    deactivate server

    Note right of browser: Browser executes JavaScript to render the updated notes
